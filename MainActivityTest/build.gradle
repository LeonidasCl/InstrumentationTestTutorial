buildscript {
    repositories {
        mavenCentral()
        flatDir {
            dirs 'libs'
        }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.5.0'
    }
}

apply plugin: 'com.android.application'
repositories {
	mavenCentral()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    androidTestCompile fileTree(dir: 'libs', include: 'robotium-solo-5.5.4.jar')
}

android {
    compileSdkVersion 'android-10'
    buildToolsVersion '19.1'

    android {
        defaultConfig {
            minSdkVersion 17
        }

        lintOptions {
            abortOnError false
        }
        sourceSets
                {
                    androidTest
                            {
                                res.srcDirs = ['res']
                                assets.srcDirs = ['assets']
                            }
                }
    }
    buildTypes {
        release {
            proguardFile getDefaultProguardFile('proguard-android.txt')
        }
    }
}

task assembleDebug(overwrite: true) {}

task copyTask(type: Copy) {
    from 'F:/Workspace/Developments/android/testOFtests/app/app-release_debug.apk'
    into 'build/outputs/apk/'
    rename {
        'MainActivityTest-debug.apk'
    }
}

assembleDebug.dependsOn copyTask